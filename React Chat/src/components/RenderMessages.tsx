import React, { FC } from 'react';
import { ChatMessages } from '../assets/Types';

const RenderMessages: FC<ChatMessages> = (msgs) => {

    const keys = Object.keys(msgs);

    //console.log(keys);

    return <div></div>
}

export default RenderMessages;